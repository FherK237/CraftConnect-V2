import { MockServer } from "./MockServer.js";
interface MockServerOptions {
    baseUrl?: string;
}
declare class MockServerPool {
    private servers;
    createServer(options?: Partial<MockServerOptions>): MockServer;
    getServers(): MockServer[];
    listen(): void;
    close(): void;
}
export declare const mockServerPool: MockServerPool;
export {};
