import { afterAll, beforeAll } from "@jest/globals";
import { mockServerPool } from "./MockServerPool.mjs";
beforeAll(() => {
    mockServerPool.listen();
});
afterAll(() => {
    mockServerPool.close();
});
