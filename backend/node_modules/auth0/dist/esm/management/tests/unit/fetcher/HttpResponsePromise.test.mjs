var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import { beforeEach, describe, expect, it, jest } from "@jest/globals";
import { HttpResponsePromise } from "../../../../../src/management/core/fetcher/HttpResponsePromise.mjs";
describe("HttpResponsePromise", () => {
    const mockRawResponse = {
        headers: new Headers(),
        redirected: false,
        status: 200,
        statusText: "OK",
        type: "basic",
        url: "https://example.com",
    };
    const mockData = { id: "123", name: "test" };
    const mockWithRawResponse = {
        data: mockData,
        rawResponse: mockRawResponse,
    };
    describe("fromFunction", () => {
        it("should create an HttpResponsePromise from a function", () => __awaiter(void 0, void 0, void 0, function* () {
            const mockFn = jest
                .fn()
                .mockResolvedValue(mockWithRawResponse);
            const responsePromise = HttpResponsePromise.fromFunction(mockFn, "arg1", "arg2");
            const result = yield responsePromise;
            expect(result).toEqual(mockData);
            expect(mockFn).toHaveBeenCalledWith("arg1", "arg2");
            const resultWithRawResponse = yield responsePromise.withRawResponse();
            expect(resultWithRawResponse).toEqual({
                data: mockData,
                rawResponse: mockRawResponse,
            });
        }));
    });
    describe("fromPromise", () => {
        it("should create an HttpResponsePromise from a promise", () => __awaiter(void 0, void 0, void 0, function* () {
            const promise = Promise.resolve(mockWithRawResponse);
            const responsePromise = HttpResponsePromise.fromPromise(promise);
            const result = yield responsePromise;
            expect(result).toEqual(mockData);
            const resultWithRawResponse = yield responsePromise.withRawResponse();
            expect(resultWithRawResponse).toEqual({
                data: mockData,
                rawResponse: mockRawResponse,
            });
        }));
    });
    describe("fromExecutor", () => {
        it("should create an HttpResponsePromise from an executor function", () => __awaiter(void 0, void 0, void 0, function* () {
            const responsePromise = HttpResponsePromise.fromExecutor((resolve) => {
                resolve(mockWithRawResponse);
            });
            const result = yield responsePromise;
            expect(result).toEqual(mockData);
            const resultWithRawResponse = yield responsePromise.withRawResponse();
            expect(resultWithRawResponse).toEqual({
                data: mockData,
                rawResponse: mockRawResponse,
            });
        }));
    });
    describe("fromResult", () => {
        it("should create an HttpResponsePromise from a result", () => __awaiter(void 0, void 0, void 0, function* () {
            const responsePromise = HttpResponsePromise.fromResult(mockWithRawResponse);
            const result = yield responsePromise;
            expect(result).toEqual(mockData);
            const resultWithRawResponse = yield responsePromise.withRawResponse();
            expect(resultWithRawResponse).toEqual({
                data: mockData,
                rawResponse: mockRawResponse,
            });
        }));
    });
    describe("Promise methods", () => {
        let responsePromise;
        beforeEach(() => {
            responsePromise = HttpResponsePromise.fromResult(mockWithRawResponse);
        });
        it("should support then() method", () => __awaiter(void 0, void 0, void 0, function* () {
            const result = yield responsePromise.then((data) => (Object.assign(Object.assign({}, data), { modified: true })));
            expect(result).toEqual(Object.assign(Object.assign({}, mockData), { modified: true }));
        }));
        it("should support catch() method", () => __awaiter(void 0, void 0, void 0, function* () {
            var _a;
            const errorResponsePromise = HttpResponsePromise.fromExecutor((_, reject) => {
                reject(new Error("Test error"));
            });
            const catchSpy = jest.fn();
            yield errorResponsePromise.catch(catchSpy);
            expect(catchSpy).toHaveBeenCalled();
            const error = (_a = catchSpy.mock.calls[0]) === null || _a === void 0 ? void 0 : _a[0];
            expect(error).toBeInstanceOf(Error);
            expect(error.message).toBe("Test error");
        }));
        it("should support finally() method", () => __awaiter(void 0, void 0, void 0, function* () {
            const finallySpy = jest.fn();
            yield responsePromise.finally(finallySpy);
            expect(finallySpy).toHaveBeenCalled();
        }));
    });
    describe("withRawResponse", () => {
        it("should return both data and raw response", () => __awaiter(void 0, void 0, void 0, function* () {
            const responsePromise = HttpResponsePromise.fromResult(mockWithRawResponse);
            const result = yield responsePromise.withRawResponse();
            expect(result).toEqual({
                data: mockData,
                rawResponse: mockRawResponse,
            });
        }));
    });
});
