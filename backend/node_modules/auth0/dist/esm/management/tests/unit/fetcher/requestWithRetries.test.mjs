var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import { requestWithRetries } from "../../../../../src/management/core/fetcher/requestWithRetries.mjs";
describe("requestWithRetries", () => {
    let mockFetch;
    let originalMathRandom;
    let setTimeoutSpy;
    beforeEach(() => {
        mockFetch = jest.fn();
        originalMathRandom = Math.random;
        // Mock Math.random for consistent jitter
        Math.random = jest.fn(() => 0.5);
        jest.useFakeTimers({ doNotFake: ["nextTick"] });
    });
    afterEach(() => {
        Math.random = originalMathRandom;
        jest.clearAllMocks();
        jest.clearAllTimers();
    });
    it("should retry on retryable status codes", () => __awaiter(void 0, void 0, void 0, function* () {
        setTimeoutSpy = jest.spyOn(global, "setTimeout").mockImplementation((callback) => {
            process.nextTick(callback);
            return null;
        });
        const retryableStatuses = [408, 429, 500, 502];
        let callCount = 0;
        mockFetch.mockImplementation(() => __awaiter(void 0, void 0, void 0, function* () {
            if (callCount < retryableStatuses.length) {
                return new Response("", { status: retryableStatuses[callCount++] });
            }
            return new Response("", { status: 200 });
        }));
        const responsePromise = requestWithRetries(() => mockFetch(), retryableStatuses.length);
        yield jest.runAllTimersAsync();
        const response = yield responsePromise;
        expect(mockFetch).toHaveBeenCalledTimes(retryableStatuses.length + 1);
        expect(response.status).toBe(200);
    }));
    it("should respect maxRetries limit", () => __awaiter(void 0, void 0, void 0, function* () {
        setTimeoutSpy = jest.spyOn(global, "setTimeout").mockImplementation((callback) => {
            process.nextTick(callback);
            return null;
        });
        const maxRetries = 2;
        mockFetch.mockResolvedValue(new Response("", { status: 500 }));
        const responsePromise = requestWithRetries(() => mockFetch(), maxRetries);
        yield jest.runAllTimersAsync();
        const response = yield responsePromise;
        expect(mockFetch).toHaveBeenCalledTimes(maxRetries + 1);
        expect(response.status).toBe(500);
    }));
    it("should not retry on success status codes", () => __awaiter(void 0, void 0, void 0, function* () {
        setTimeoutSpy = jest.spyOn(global, "setTimeout").mockImplementation((callback) => {
            process.nextTick(callback);
            return null;
        });
        const successStatuses = [200, 201, 202];
        for (const status of successStatuses) {
            mockFetch.mockReset();
            setTimeoutSpy.mockClear();
            mockFetch.mockResolvedValueOnce(new Response("", { status }));
            const responsePromise = requestWithRetries(() => mockFetch(), 3);
            yield jest.runAllTimersAsync();
            yield responsePromise;
            expect(mockFetch).toHaveBeenCalledTimes(1);
            expect(setTimeoutSpy).not.toHaveBeenCalled();
        }
    }));
    it("should apply correct exponential backoff with jitter", () => __awaiter(void 0, void 0, void 0, function* () {
        setTimeoutSpy = jest.spyOn(global, "setTimeout").mockImplementation((callback) => {
            process.nextTick(callback);
            return null;
        });
        mockFetch.mockResolvedValue(new Response("", { status: 500 }));
        const maxRetries = 3;
        const expectedDelays = [1000, 2000, 4000];
        const responsePromise = requestWithRetries(() => mockFetch(), maxRetries);
        yield jest.runAllTimersAsync();
        yield responsePromise;
        // Verify setTimeout calls
        expect(setTimeoutSpy).toHaveBeenCalledTimes(expectedDelays.length);
        expectedDelays.forEach((delay, index) => {
            expect(setTimeoutSpy).toHaveBeenNthCalledWith(index + 1, expect.any(Function), delay);
        });
        expect(mockFetch).toHaveBeenCalledTimes(maxRetries + 1);
    }));
    it("should handle concurrent retries independently", () => __awaiter(void 0, void 0, void 0, function* () {
        setTimeoutSpy = jest.spyOn(global, "setTimeout").mockImplementation((callback) => {
            process.nextTick(callback);
            return null;
        });
        mockFetch
            .mockResolvedValueOnce(new Response("", { status: 500 }))
            .mockResolvedValueOnce(new Response("", { status: 500 }))
            .mockResolvedValueOnce(new Response("", { status: 200 }))
            .mockResolvedValueOnce(new Response("", { status: 200 }));
        const promise1 = requestWithRetries(() => mockFetch(), 1);
        const promise2 = requestWithRetries(() => mockFetch(), 1);
        yield jest.runAllTimersAsync();
        const [response1, response2] = yield Promise.all([promise1, promise2]);
        expect(response1.status).toBe(200);
        expect(response2.status).toBe(200);
    }));
});
